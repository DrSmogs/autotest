#!/usr/bin/python
# -*- coding: utf-8 -*-

#Config file - saves settings for logging in. This should be part of .gitignore so it's not published
#
class config:

    loginjid = '<username>@<domain>.com' #username for the app to log into xmpp server
    loginpw = 'Password' # pass word for app to logiunn to xmpp server
    resource = 'iq3' # resource for the iQ3 unit - will alkways be iQ3
    apiusername = 'apiusername' #username to access the api
    apipassword = 'apipassword' #password to access the api
